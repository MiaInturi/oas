openapi: 3.1.0
info:
  title: discriminator mapping fixture
  version: 1.0.0
paths:
  /events:
    get:
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                $ref: ./schemas/EventBase.yaml
