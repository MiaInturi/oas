openapi: 3.0.3
info:
  version: '1.0'
  title: Petstore definition with a nullish example value
paths:
  /pets:
    get:
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: ./schemas/Pet.yaml
              example:
                data:
                  id:
                  name: snowball
